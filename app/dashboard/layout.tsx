export const metadata = {
	title: {
		template: "%s | Dashboard",
		default: "SIE | Dashboard",
	},
};

export default function DashboardLayout({ children }: { children: React.ReactNode }) {
	return children;
}
